<!DOCTYPE html>
<html>
  <head>
    <title>JokeApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <script src="https://cdn.jsdelivr.net/npm/js-cookie@2/src/js.cookie.min.js"></script>
  </head>

  <body>
    <div class="container text-center">
      <%= content_tag :div, notice, id: "alert-message", class: "alert alert-success", role: "alert" if notice.present? %>
      <%= content_tag :div, alert, id: "alert-message", class: "alert alert-danger", role: "alert" if alert.present? %>
    </div>
    <%= render "layouts/header" %>
    <%= yield %>
    <%= render "layouts/footer" %>
    <% if cookies[:allow_cookies].blank? %>
      <%= render "layouts/cookies_bar" %>
    <% end %>
  </body>
</html>
